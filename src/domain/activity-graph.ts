/** Canonical graph contract shared by Gemini, UI and persistence. */
export {
  ActivityGraphSchema,
  type ActivityGraph,
  type ActivityNode,
  type ActivityEdge,
  type ActivityWarning,
} from '../lib/activity/schema'
